// Questo file non è più usato: la dashboard ora legge la configurazione da
// /static/js/supabase-config.js, condivisa anche con il menu clienti.
// Modifica quel file per collegare Supabase (non serve più toccare questo).
